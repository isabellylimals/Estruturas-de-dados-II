#include<stdio.h>
 int fibonacci (int n ) {
 if ( n <= 1) {
 return n ;
 }
 else {
 return fibonacci ( n - 1) + fibonacci ( n - 2) ;
 }
}
int main () {
     fibonacci (15) ;
     return 0; }

    /* 
a) Depure o código para descobrir quantas vezes o fibonacci(5) é calculado (requisitado). 89 vezes, iria mais se eu continuasse.
b) Descubra através da depuração, qual a maior quantidade de níveis (frames) esse código alcança.
   Comandos:
      set var count=0
      commands 1
      silent
      if n==5
      set $count=$count+1
      end
      end
      run, next, next, c
      print $count
   16 vezes, usando o backtrace.

c) Depure o código até o momento que o primeiro fibonacci(14) é calculado e o executável irá começar
   a calcular o fibonacci(13) (usei dois steps).
*/
